# Host port mapped to the web container's 8000
HOST_PORT=8000
# Per-chunk settle delay (seconds) before replacing an abnormal block during
# repair; keeps the REPAIRING audit window observable. 0 disables the delay.
REPAIR_CHUNK_DELAY=0.25
